
public class stringClass {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		//String is a reference data type that has access to built in methods
		
		String name = "Simon";
		
		boolean result = name.equalsIgnoreCase("simon"); //returns boolean value
//		int result = name.length(); //returns number 
//		char result = name.charAt(0); //grab a letter at index of string
//		int result = name.indexOf(ch); returns the location of the character found
//		boolean result = name.isEmpty(); // checks for uninstantiated variable
//		String result = name.toUpperCase(); //all uppercase letters now
//		String result = name.toLowerCase(); // all lowercase letters now
		
//		String result = name.trim(); //removes all empty space
//		String result = name.replace(oldChar, newChar);  // basically a find and replace cmd
		
		
		System.out.println(result);
		
	}

}
