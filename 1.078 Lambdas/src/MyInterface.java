@FunctionalInterface
public interface MyInterface {
	//we turn this into a funcitonal interface
	
	//a functional interface contains only one abstract method
	
	public void message(String string, String string2);
	
}
