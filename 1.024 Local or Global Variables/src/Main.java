
public class Main {

	public static void main(String[] args) {
		//local = declared inside a method
			      //scope is within that method only
		//global = declared outside methods but within a class
				  //scope is within entire class
		
		//creates the diceObject and calls the constructor
		DiceRoller diceRoller = new DiceRoller(); 
	}

}
